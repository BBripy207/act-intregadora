version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    command: [ "npm", "start" ]
  frontend:
    build: ./frontend
    ports:
      - "3001:3000"
    command: [ "npm", "start" ]
    environment:
      - REACT_APP_BACKEND_URL=http://backend:3000
